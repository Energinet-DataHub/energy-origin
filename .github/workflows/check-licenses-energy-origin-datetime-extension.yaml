name: Check Licenses - EnergyOriginDateTimeExtension
on:
  pull_request:
    branches: [ main ]
    paths:
      - libraries/dotnet/EnergyOriginDateTimeExtension/**
jobs:
  check-licenses:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Check license
      uses: Energinet-DataHub/.github/.github/actions/dotnet-check-license@main
      with:
        project-folder: libraries/dotnet/EnergyOriginDateTimeExtension/EnergyOriginDateTimeExtension
        license-mapping-file: .mapped-licenses.json
