name: SonarCloud
on:
  workflow_call:
    paths-ignore:
      - domains/certificates/**
jobs:
  sonar-analysis:
    name: sonar-analysis
    runs-on: ubuntu-latest
    steps:
      - run: echo "::notice ::No Sonarcloud scan required"

