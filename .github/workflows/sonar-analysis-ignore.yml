name: SonarCloud
on:
  pull_request:
    paths-ignore:
      - domains/certificates/**
jobs:
  sonar-analysis:
    name: sonar-analysis
    runs-on: ubuntu-latest
    steps:
      - run: echo "::notice ::No Sonarcloud scan required"

