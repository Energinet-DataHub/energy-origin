name: SonarCloud
on:
  pull_request:
    paths-ignore:
      - domains/auth/**
      - domains/emissions/**
      - domains/measurements/**
      - domains/oidc-mock/**
jobs:
  sonar-analysis:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No SonarCloud scan required"'
