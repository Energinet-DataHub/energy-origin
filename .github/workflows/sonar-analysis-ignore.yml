name: SonarCloud
on:
  pull_request:
    paths-ignore:
      - domains/auth/**
      - domains/emissions/**
      - domains/measurements/**
      - domains/oidc-mock/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No SonarCloud required"'
