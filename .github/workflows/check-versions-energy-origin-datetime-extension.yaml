name: Version Check - EnergyOriginDateTimeExtension
on:
  pull_request:
    branches: [ main ]
    paths:
      - libraries/dotnet/EnergyOriginDateTimeExtension/**
jobs:
  check_version:
    name: Check version
    uses: Energinet-DataHub/.github/.github/workflows/version-increment-check.yaml@main
    with:
      yaml_file: libraries/dotnet/EnergyOriginDateTimeExtension/version.yaml
      yaml_path: version
