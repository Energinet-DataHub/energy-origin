apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-rabbitmq
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: rbmq
      port: 5672
      protocol: TCP
      targetPort: 5672
  selector:
    app: {{ .Release.Name }}-rabbitmq
  type: ClusterIP
