apiVersion: v1
kind: Service
metadata:
  name: eo-data-sync
  namespace: eo
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app: data-sync-api
  type: ClusterIP
