apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: datahub2-api-endpoint-secret
  namespace: eo
spec:
  encryptedData:
    endpoint: {{ required "datahub.endpoint is required" .Values.datahub.endpoint | quote}}
