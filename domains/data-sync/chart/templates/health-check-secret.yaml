apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: datahub2-endpoint-secret
  namespace: eo
spec:
  encryptedData:
    endpoint: {{ required "datahub.health_check.endpoint is required" .Values.datahub.health_check.endpoint | quote}}
