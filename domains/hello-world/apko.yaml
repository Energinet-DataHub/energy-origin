contents:
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  repositories:
    - https://packages.wolfi.dev/os
    - '@local /packages'
  packages:
    - wolfi-baselayout
    - hello-world@local
    - busybox

entrypoint:
  command: /hello

archs:
  - amd64
