{{ if .Values.enabled }}

apiVersion: v1
kind: Service
metadata:
  name: eo-datahub2-api-mock-service
spec:
  type: ClusterIP
  selector:
    app: datahub2-api-mock
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
{{ end }}
