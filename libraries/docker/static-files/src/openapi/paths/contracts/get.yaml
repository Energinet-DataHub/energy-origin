tags:
  - Contracts
summary: Returns all the user's contracts for issuing granular certificates
operationId: getContracts
parameters:
  - $ref: '../../components/headers/version.yaml'
responses:
  '200':
    description: Success
    content:
      application/json:
        schema:
          type: object
          properties:
            result:
              type: array
              items:
                $ref: '../../components/schemas/certificate/Contract.yaml'
