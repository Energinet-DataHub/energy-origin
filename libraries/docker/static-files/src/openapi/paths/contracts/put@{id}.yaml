tags:
  - Contracts
summary: Edit the end date for contract
operationId: updateContractEndDate
parameters:
  - name: id
    in: path
    required: true
    schema:
      type: string
      format: uuid
  - $ref: '../../components/headers/version.yaml'
requestBody:
  content:
    application/json:
      schema:
        $ref: '../../components/schemas/certificate/EditContractEndDate.yaml'
    application/*+json:
      schema:
        $ref: '../../components/schemas/certificate/EditContractEndDate.yaml'
responses:
  '200':
    description: Success
  '403':
    description: Forbidden
  '404':
    description: Not Found
